/google-drive-mcp
/google-drive-mcp.exe
# `go build ./scripts/X` drops its output here under the package name.
# Use `go run` instead; these are so a slip cannot be committed.
/gates
/livedrive
/dist/
cov.out
schemas.json
*.test
client_secret*.json
token.json
.env
# The editor writes its settings and its temporary copies of them into
# the working tree while a session runs, so a wildcard `git add` takes
# whatever exists at that instant. A tool that writes here needs its rule
# before the first wildcard add, not after the first one is committed.
.claude/settings.local.json
.claude/settings.local.json.tmp.*
